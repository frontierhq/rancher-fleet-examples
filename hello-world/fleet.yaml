---
namespace: hello-world
targetCustomizations:
  - clusterSelector: {}
    helm:
      values:
        # clusterName: global.fleet.clusterLabels.management.cattle.io/cluster-name
        clusterName: "${ .ClusterLabels.management.cattle.io/cluster-name }"
